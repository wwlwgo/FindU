# 找到你：由个人 Agent 协商形成的黑客松连接

## 1. 一句话定义

参与者用一段 1-2 分钟的语音建立由本人确认的个人 Agent；Agent 代表主人在活动网络中广播、接触、提问和协商，形成少量候选交流意向，再由真人决定是否见面。

第一版只帮助参与者认识值得交流的人，不直接组建队伍。组队是双方见面后的后续动作。

这不是让一个中央大模型替所有人分配队友，而是让多个拥有不同私有信息、目标和决策权的 Agent，通过真实交互形成双向共识。

## 2. 我们要解决的问题

开放跨职能黑客松中，参与者常遇到三个问题：

- 很难在短时间内说清楚自己能提供什么、真正需要什么；
- 内向或不善于主动社交的人容易错过合适的连接；
- 传统标签匹配只看相似度，无法确认双方是否真的互有价值、时间和合作方式是否兼容。

我们的目标不是替人决定队友，而是让个人 Agent 先完成高成本的发现、破冰和初步协商，把少量值得真人交流的连接交给主人确认。

## 3. 核心原则

1. **主人控制画像。** AI 可以大胆推断，但推断项默认不生效，必须展示依据并由用户逐条确认。
2. **公开信息与私有偏好分离。** 用户决定一项信息是公开广播、仅供自己的 Agent 决策，还是完全不使用。这里的“私有”表示不会披露给其他参与者或用于中央评审；平台和所调用的模型仍需处理它，MVP 必须明确告知这一点。
3. **每个 Agent 独立决策。** 每个 Agent 有独立画像、目标、底线、记忆和模型调用，不存在替所有人下结论的中央评审 Agent。
4. **匹配来自双向意向。** “系统认为合适”不等于匹配成功；只有双方 Agent 分别作出积极决定，才进入真人确认。
5. **有限自主，而非无限聊天。** Agent 可以自主选择对象和问题，但受到轮次、联系人数、信息权限和安全规则约束。
6. **解释价值，不伪造精度。** 不展示没有统计依据的匹配百分比，只呈现双方能交换什么、协商发现了什么、还有什么风险。
7. **人拥有最终决定权。** Agent 达成的只是候选意向，真人可以接受、拒绝或继续提问。

## 4. 完整产品流程

### 阶段 A：语音建立个人 Agent

参与者进入专属手机网页，录制 1-2 分钟自我介绍。引导问题包括：

- 你正在做什么，擅长什么？
- 你在这次活动中能帮助别人什么？
- 你希望从别人那里获得什么？
- 你偏好的工作方式和可投入时间是什么？
- 你目前最不确定的问题是什么？

模型从语音中生成三类内容：

- **客观背景：** 技能、角色、经历、可投入时间；
- **价值供给与需求：** 可以分享的知识或经验，以及想获得的信息或能力；
- **推断需求：** 与本次活动直接相关、可由其他参与者回答，并附有原话依据的潜在深层需求。

### 阶段 B：用户确认与权限设置

参与者逐条修改或确认画像，并为每项内容选择权限：

- **公开：** Agent 可以在广播或私聊中主动表达；
- **私有：** 只能用于自己的 Agent 判断，不得向其他 Agent 直接披露；
- **禁用：** 不进入 Agent 上下文。

确认后删除原始音频和完整转写，只保留用户确认过的结构化画像、授权摘要和必要的审计信息。实际产品上线前还需确认模型服务商的数据保留政策。

### 阶段 C：Agent 广播与自主发现

每个 Agent 根据公开信息生成一条对外广播，例如：

> 我擅长前端和交互设计，正在探索 AI 协作产品，希望认识能快速搭建后端原型，或做过黑客松范围控制的人。

Agent 阅读其他人的公开广播，根据自己主人的目标和私有偏好，自主决定最多联系 3 名候选人。平台只负责发布广播、传递消息和执行频率限制，不判断谁应该联系谁。

### 阶段 D：Agent-to-Agent 有限协商

每对 Agent 通过协议化消息完成最多 3 轮沟通：

一轮定义为“一方发起一个动作，对方完成一次响应”。对方的成功回复、明确 `DECLINE` 或超时都会结束这一轮；`PROPOSE` 与 `ACCEPT` 或 `DECLINE` 也计为一轮。推荐的三轮结构是：首次接触与回应、关键澄清与回应、候选意向与接受或拒绝。

1. **CONTACT：** 主动说明为什么联系对方；
2. **QUESTION / ANSWER：** 询问公开画像无法判断的关键问题；
3. **CLARIFY：** 澄清时间、角色、工作方式或信息需求；
4. **PROPOSE：** 提出见面或进一步交流的候选意向；
5. **ACCEPT / DECLINE：** 对方独立接受或拒绝。

每次模型调用只获得该 Agent 自己的画像、私有规则、已收到的消息和该段对话记忆。它不能读取其他 Agent 的私有画像，也不能看到全场的隐藏评价。

### 阶段 E：真人确认

产品中的状态必须严格区分：

```text
单方 Agent 提案
→ 双方 Agent 意向（生成候选推荐卡）
→ 一方真人确认（等待对方）
→ 双方真人确认（连接成立）
```

只有双方 Agent 都表达积极意向时，系统才生成推荐卡。此时只是候选连接，不称为“匹配成功”。双方真人分别确认后连接才成立。若现场只有一位真实演示用户，Demo 必须停在“已由一方确认，等待对方确认”，不能把预设 Agent 的响应伪装成真人确认；要展示完整闭环，需要两名真人测试用户。推荐卡展示：

- 他能给你什么；
- 你能给他什么；
- Agent 通过交流新确认了什么；
- 仍需真人确认的风险或分歧；
- 为什么值得聊；
- 建议的第一句话。

每人最多收到 5 个高质量候选连接；没有达到条件时可以少于 5 个，不强行凑数。真人双方确认后，平台再交换联系方式或创建会面安排。组队不属于第一版流程。

## 5. 系统中“中央部分”的边界

平台可以有中央基础设施，但不能有中央裁判。

平台负责：

- Agent 注册、消息投递和状态存储；
- 限制联系数量、轮次、超时和恶意内容；
- 限制单个参与者被广播曝光或被联系的次数；
- 判断是否完成双向握手；
- 把双方各自生成的理由整理为推荐卡。

平台不负责：

- 汇总所有私有画像并统一排名；
- 生成全场唯一的匹配分数；
- 替某个 Agent 接受或拒绝；
- 为了填满名额而强制分配关系。

热门参与者的曝光控制只约束消息可达性，不改变 Agent 的内部判断。MVP 采用以下硬限制：

- 每个 Agent 在同一匹配窗口内最多主动联系 3 名不同参与者；
- 每名参与者在同一匹配窗口内最多接收 5 个不同 Agent 的主动联系；
- 每对 Agent 最多交流 3 轮；
- 接收联系数达到 5 后，该参与者不再出现在新的可联系候选中，并显示“当前忙碌”；
- MVP 不做等待队列；已有会话可以完成，但不能新增会话。

一次 `CONTACT` 创建会话时，平台以原子操作登记目标参与者的一次入站联系；第 6 次并发请求必须失败。被拒绝、超时或结束的会话不会释放名额，名额只在下一匹配窗口重置，避免热门参与者被反复触达。

每个 Agent 最多保留 5 个候选交流意向。超过 5 个时，该 Agent 必须依据主人的私有偏好自行保留 5 个、拒绝或结束其余意向；平台只执行状态变更，不负责排序或替 Agent 选择。这与“每人最多收到 5 张候选推荐卡”保持一致。

“匹配窗口”默认为主办方发起的一次活动前匹配批次。这些限制必须由平台代码执行，不能只写进 Prompt。

## 6. 为什么不能被“一次性扔给豆包”替代

一次性中央匹配拥有全部信息，并从单一视角直接输出结论；本方案的结果来自多个独立主体在局部信息下逐步形成的选择。

具体差异包括：

- 同一个候选人会被不同 Agent 基于不同目标作出不同判断；
- 私有偏好不需要暴露给中央评审或其他参与者；
- Agent 可以通过追问获得原始画像中没有的信息，并据此改变决定；
- 一方感兴趣不构成匹配，必须由另一方独立响应；
- 拒绝、保留、反问和附条件接受都是有效结果；
- 推荐可以明确说明是哪段协商让双方产生了意向。

第一版不制作中央 LLM 对照组。产品通过 Agent 的独立私有状态、主动追问、判断变化和双向意向过程说明自身机制。

## 7. 四小时 MVP

四小时版本的最小成功闭环是：一个现场用户完成真实录音和画像确认，其 Agent 浏览四个预设 Agent 的广播，最多主动联系其中 3 个，并通过受控协商产生至少一次拒绝和一次双向意向，最后由真人确认。除这个闭环外的能力都可以缓存或预计算。

单人演示停在“一方真人已确认，等待另一方确认”；两人现场演示时才展示“连接成立”。

### 必须完成

- 4 个已经准备好画像和广播的预设参与者，以及 1 个可现场录入的新参与者；
- 新参与者的真实语音录制或上传，以及转写结果；
- 可直接选择预设转写文本的演示兜底入口；
- 新参与者的三类画像和至少一条带依据的推断需求；
- 新参与者逐条确认并设置公开/私有/禁用；
- 每个 Agent 独立上下文、记忆和模型调用；
- 使用状态机约束的广播、主动联系、问答、提案、接受或拒绝；
- 可视化 Agent 消息流；
- 能在单张关键截图中完整呈现三轮交流、判断变化和双方意向；
- 双向意向推荐卡和真人确认按钮；
- 模型失败时可回放的固定 Demo 数据；

### 暂不实现

- 完整账号和权限系统；
- 长期记忆或跨活动身份；
- 数十个 Agent 无限并发自由聊天；
- 向量数据库和复杂推荐模型；
- 全局最优组队算法；
- 自动交换真实联系方式；
- 生产级隐私合规和内容安全系统。

### 建议时间分配

| 时间 | 工作 |
|---|---|
| 0:00-0:30 | 锁定数据结构、Agent 行为协议和演示角色 |
| 0:30-1:20 | 完成语音转写、画像提取、推断确认 |
| 1:20-2:20 | 完成独立 Agent、消息总线和有限轮协商 |
| 2:20-3:20 | 完成移动端流程、消息流和推荐卡 |
| 3:20-4:00 | 联调、制作回放兜底、排练 Demo |

## 8. Demo 剧本

1. 现场录制一段自我介绍。
2. AI 提取公开画像，并推断一条隐藏需求，例如“可能需要有人帮助缩小项目范围”。
3. 用户查看推断依据，确认该需求仅供自己的 Agent 决策。
4. 现场用户 Agent 与四个预设 Agent 共发布五条不同广播。
5. 新用户的 Agent 主动联系一名后端 Agent，并追问可投入时间。
6. 另一名 Agent 主动联系新用户，但因工作节奏冲突被礼貌拒绝。
7. 后端 Agent 反问新用户能否承担产品验证，得到确认后提出交流意向。
8. 双方 Agent 完成 ACCEPT，系统生成推荐卡。
9. 真人查看完整理由和待确认问题，点击“愿意见面”。

Demo 的关键不是聊天气泡数量，而是清楚展示：每条消息为什么被发送、它改变了哪个 Agent 的判断，以及最终意向来自双方独立决定。演示页面应支持在一张截图中看清三轮交流、关键新增信息、判断变化和当前确认状态。

## 9. 核心数据契约

```json
{
  "agent_id": "alice",
  "public_profile": {
    "background": ["frontend", "UX"],
    "can_offer": ["快速制作交互原型"],
    "seeking": ["后端实现", "范围控制经验"]
  },
  "private_preferences": [
    {
      "claim": "希望队友能快速试错并明确分工",
      "evidence": "用户提到时间很短，不想反复争论方向",
      "confirmed": true
    }
  ],
  "conversation_state": {},
  "limits": {
    "max_contacts": 3,
    "max_inbound_contacts": 5,
    "max_turns_per_peer": 3,
    "max_candidate_intents": 5
  }
}
```

Agent 每一步只输出一个结构化动作：

```json
{
  "action": "question",
  "recipient": "bob",
  "message": "你在活动期间可以投入多长时间？",
  "private_reason": "时间投入是主人确认过的底线",
  "interest": "continue"
}
```

`private_reason` 只用于自己的 Agent 记忆和主人侧调试视图，不发送给对方，也不直接出现在公开 Demo 中。公开推荐理由需要基于双方允许披露的信息另行生成。

## 10. 实验指标

核心指标不是模型给出的“匹配准确率”，而是 Agent 是否帮助用户建立了真实、有价值的连接：

1. 推断需求确认率：用户主动采纳了多少带依据的深层需求；
2. 信息增量：Agent 协商补充了多少原始画像中不存在的关键信息；
3. 协商贡献率：多少双向意向是在追问或澄清之后形成的；
4. 真人接受率：双向 Agent 意向有多少得到双方真人确认；
5. 有效交流率：会面后双方是否都获得了具体帮助或信息；
6. 公平性：曝光和有效连接是否过度集中在少数热门参与者；

## 11. 已确认的产品决策

1. 第一版只帮助参与者认识值得交流的人，组队是后续动作。
2. Agent 可以提出候选交流意向，但必须由真人确认。
3. 每个 Agent 最多主动联系 3 人，每对 Agent 最多交流 3 轮；三轮交流及判断变化必须能被清楚截图展示。
4. 保留带依据的深层需求推断；推断默认不生效，经用户确认后才供 Agent 使用。
5. MVP 同时提供真实语音 API 和预设转写入口；前者用于真实体验，后者用于稳定演示和故障兜底。
6. 第一版不制作中央 LLM 对照组。
7. 热门参与者采用硬限制：同一匹配窗口最多接收 5 个不同 Agent 的主动联系，达到上限后停止新增触达并显示“当前忙碌”。
8. 必须准备完整的预设 Agent、固定消息轨迹和缓存结果，应对语音或模型服务失败。

## 12. 对外表达

> “找到你”不是让 AI 替人决定和谁交流。每个参与者先通过语音建立一个由本人确认、拥有私有偏好和独立记忆的个人 Agent。Agent 在活动网络中代表主人主动发现、提问和协商，只有双方 Agent 都产生意向时才推荐真人见面。我们把社交中最费力的发现和破冰交给 Agent，把最终选择留给人。第一版促成交流，是否组队由真人见面后决定。
>
